#include <bits/stdc++.h>

using namespace std;

int main() {
    string names[6];
    for (int i = 0; i < 6; ++i)
        cin >> names[i];
    set <string> ans(names, names + 6);
    cout << (ans.size() == 3 ? "Run Solo" : "Hutt gambit") << '\n';
    return 0;
}
