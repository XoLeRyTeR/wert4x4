#include <bits/stdc++.h>

using namespace std;

int main() {
    int d;
    string m;
    cin >> d >> m;
    set <string> winter({"january", "february", "december"});
    cout << (winter.count(m) == 1 ? "Yes" : "No") << '\n';
    return 0;
}
