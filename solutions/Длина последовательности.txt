#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, ans = 0;
    while (cin >> n) {
        if (n == 0) {
            cout << ans << '\n';
            return 0;
        }
        ++ans;
    }
    return 0;
}
