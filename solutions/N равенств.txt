#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    cin >> n;
    const int N = n + 1;
    for (int i = 0; i < n; ++i)
        cout << i + 1 << " + " << n - i << " = " << N << '\n';
    return 0;
}
