#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    cin >> n;
    for (int i = 0; i < n; ++i) {
        char h;
        int s, m;
        cin >> h >> s >> m;
        cout << string(s, ' ') << string(m, h) << '\n';
    }
    return 0;
}
