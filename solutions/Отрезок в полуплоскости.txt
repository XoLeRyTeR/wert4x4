#include <bits/stdc++.h>
#define sign(X) (X == 0 ? 0 : X < 0 ? -1 : 1)
using namespace std;

int main() {
    int x1, y1, x2, y2;
    cin >> x1 >> y1 >> x2 >> y2;
    cout << (sign(x1) != sign(x2) ? "cross" : sign(x1) < 0 ? "left" : "right") << '\n';
    return 0;
}
