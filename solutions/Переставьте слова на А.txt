#include <bits/stdc++.h>

using namespace std;

int main() {
    string s;
    while (cin >> s) {
        if (s[0] == 'A') {
            string t;
            cin >> t;
            cout << t << ' ';
        }
        cout << s << ' ';
    }
    cout << '\n';
    return 0;
}
