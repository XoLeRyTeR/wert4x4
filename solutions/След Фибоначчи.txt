#include <bits/stdc++.h>

using namespace std;

void f(int n) {
    cout << n;
    if (n >= 2) {
        f(n - 1);
        f(n - 2);
    }
}

int main() {
    int n;
    cin >> n;
    f(n);
    return 0;
}
