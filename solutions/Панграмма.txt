#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    cin >> n;
    string s;
    cin >> s;
    set <char> lets;
    for (auto let : s)
        lets.insert(tolower(let));
    cout << (lets.size() == 26 ? "YES" : "NO") << '\n';
    return 0;
}
