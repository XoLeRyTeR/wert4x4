#include <bits/stdc++.h>

using namespace std;

int main() {
    string s;
    cin >> s;
    const int n = s.size() / 3;
    for (int i = 0; i < n; ++i)
        if (s[i] == s[i + n] || s[i] == s[i + 2 * n])
            cout << s[i];
        else
            cout << s[i + n];
    return 0;
}
