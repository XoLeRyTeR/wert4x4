#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    cin >> n;
    n *= 2;
    int a[n];
    for (int i = 0; i < n; ++i)
        cin >> a[i];
    for (int i = 0; i < n; i += 2)
        cout << a[i + 1] << ' ' << a[i] << ' ';
    cout << '\n';
    return 0;
}
