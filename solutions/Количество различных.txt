#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    cin >> n;
    set <int> ans;
    for (int i = 0; i < n; ++i) {
        int num;
        cin >> num;
        ans.insert(num);
    }
    cout << ans.size() << '\n';
    return 0;
}
