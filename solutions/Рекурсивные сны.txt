#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    cin >> n;
    for (int i = 1; i <= n; ++i)
        cout << "in " << i << '\n';
    cout << "acquire data\n";
    for (int i = n; i >= 1; --i)
        cout << "out " << i << '\n';
    return 0;
}
