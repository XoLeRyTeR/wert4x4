#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    cin >> n;
    int nums[n + 1];
    nums[0] = 0;
    nums[1] = 1;
    for (int i = 2; i <= n; ++i)
        nums[i] = nums[i - 2] + nums[i - 1];
    cout << nums[n] << '\n';
    return 0;
}