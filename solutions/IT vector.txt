#include <bits/stdc++.h>

using namespace std;

int main() {
    string s;
    cin >> s;
    for (auto& let : s)
        let = tolower(let);
    cout << (s == "itvector" ? "Yes" : "No") << '\n';
    return 0;
}
