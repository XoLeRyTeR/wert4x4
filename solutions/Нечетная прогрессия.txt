#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, s;
    cin >> n >> s;
    int a = s / n - (n - 1);
    for (int i = 0; i < n; ++i)
        cout << a + i * 2 << ' ';
    cout << '\n';
    return 0;
}
