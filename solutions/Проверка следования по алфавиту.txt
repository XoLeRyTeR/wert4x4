#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    string s;
    cin >> n >> s;
    for (int i = 1; i < n; ++i)
        if (s[i] - s[i - 1] != 1) {
            cout << "NO\n";
            return 0;
        }
    cout << "YES\n";
    return 0;
}
