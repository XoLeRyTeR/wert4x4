#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    cin >> n;
    string w;
    cin >> w;
    for (auto let : w)
        if (let == 'i' || let == 'j')
            ++n;
    cout << n << '\n';
    return 0;
}
