#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    cin >> n;
    int ans = 1e6, x;
    for (int i = 0; i < n; ++i) {
        cin >> x;
        if (x % 10 == 4)
            ans = min(ans, x);
    }
    cout << ans << '\n';
    return 0;
}
