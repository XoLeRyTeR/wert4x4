#include <bits/stdc++.h>

using namespace std;

int main() {
    int o, a, b;
    cin >> o >> a >> b;
    int n = o + a + b;
    long long f[13];
    f[0] = 1;
    for (int i = 1; i <= 12; ++i)
        f[i] = f[i - 1] * i;
    cout << f[n] / (f[o] * f[a] * f[b]) << '\n';
    return 0;
}
