#include <bits/stdc++.h>

using namespace std;

int main() {
    int w, a, b;
    cin >> w >> a >> b;
    int r = a * b;
    int s = r - a;
    int t = a + r + s;
    cout << (t <= w ? "Yes" : "No") << '\n';
    cout << t << '\n';
    return 0;
}
