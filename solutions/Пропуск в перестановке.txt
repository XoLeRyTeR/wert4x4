#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

int main() {
    ll n;
    cin >> n;
    ll sum = (1 + n) * n / 2;
    for (int i = 1; i < n; ++i) {
        ll a; cin >> a;
        sum -= a;
    }
    cout << sum << '\n';
    return 0;
}
