#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    cin >> n;
    for (int i = 0; i < n; ++i) {
        string w;
        cin >> w;
        for (auto let : w)
            cout << let << ' ';
        cout << '\n';
    }
    return 0;
}
