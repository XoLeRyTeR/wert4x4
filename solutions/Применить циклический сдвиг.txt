#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, k;
    string s;
    cin >> n >> k >> s;
    for (int i = 0; i < n; ++i)
        cout << s[(i + k) % n];
    cout << '\n';
    return 0;
}
